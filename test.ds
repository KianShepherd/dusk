fn fact(n :int) -> int {
    let mut a :int = 1;
    if n <= 1 {
        a = 1;
    } else {
        a = n * fact(n - 1);
    }
    return a;
}

fn main(argc: float, argv: float) -> int {
    for (let mut a: int = 1; a < 10; a = a +1) {
        printi(fact(a));
    }
    return 1;
}
