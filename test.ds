include "math.ds"

fn main(argc: int, argv: string*) -> int {
    println("%ld", pow(2, 5));
    let base: float = 2.0;
    let power: float = 5.0;
    println("%f", fpow(base, power));
    return 0;
}
