fn fib(n: int) -> int {
    let mut a: int = 0;
    if (n <= 2) {
        a = 1;
    } else {
        a = fib(n - 1) + fib(n - 2);
    }
    return a;
}

fn main(argc: float, argv: float) -> int {
    let str: string = "%d\n";
    for (let mut i: int = 1; i < 40; i = i + 1) {
        print(str, fib(i));
    }
    return 1;
}
