struct Foo {
    a: int,
    b: int,
}


fn main(argc: int, argv: string*) -> int {
    let mut s: Foo = Foo();
    print("%ld\n", s.a);
    print("%ld\n", s.b);
    s.a = 7;
    print("%ld\n", s.a);
    return 0;
}
