fn foo(n: int) -> int {
    let mut a: int = 1;
    if n <= 1 {
        a = 1;
    } else {
        a = foo(n - 1) * n;
    }
    return a;
}

fn main(argc: float, argv: float) -> int {
    return foo(7);
}
